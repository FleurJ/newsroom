<div class="container">
  <div class="card">
    <div class="card-header">
      <%= @newsletter.title %>
    </div>
    <br>
    <div class="card-body">
      <% @newsletter.articles.each do |article|  %>
        <p> <%= link_to article.title, article_url(article) %></p>
      <% end %>
    </div>
    <p>A toi de la voir pour la vue =)</p>
  </div>


