<div class="container">
  <div class="card">
    <div class="card-header">
      Gestion des utilisateurs
    </div>
    <div class="card-body">
      <h5 class="card-title">
        <% @users.each do |user| %>
          <%= link_to user.email, edit_users_administration_path(user) %>
          <br>
        <% end %>
      </h5>
    </div>
  </div>
</div>
