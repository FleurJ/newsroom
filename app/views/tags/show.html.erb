<div class="container">
  <h1>Articles sur "<%= @tag.name %>"</h1>
  <p>Nombre d'articles sur ce thème : <%= @articles.count %></p>
  <% if @articles.empty? %>
    <p>Il n'y a pas (encore) d'article publié à cette date.</p>
  <% else %>
    <%= render 'shared/articlecards' %>
  <% end %>

</div>
