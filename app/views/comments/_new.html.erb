<%= simple_form_for([@article, @comment], remote: true) do |f| %>
    <%= f.input :body, placeholder: "Commentaire...", label: false %>
  <%= f.submit "Commenter", class: "btn btn-primary" %>
<% end %>
